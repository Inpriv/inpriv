{
  "name": "inpriv-temp-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute inpriv-temp --remote --file schema.sql",
    "test": "node test/svix.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
